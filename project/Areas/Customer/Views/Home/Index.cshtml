@{
    ViewData["Title"] = "Index";
}
@model IEnumerable<SanPham>
<style>
    .card {
        display: flex;
        flex-direction: column;
        height: 100%;
    }

    .card-body {
        flex-grow: 1;
    }

    .card-img-top {
        object-fit: contain;
        width: 100%;
        height: 200px;
    }
</style>
<div class="container">
    <br />
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-3 mb-3">
                <div class="card h-100">
                    <a asp-action="Details" asp-route-id="@item.Id">
                        <img src="@item.ImageUrl" class="card-img-top" alt="@item.Name" />
                    </a>
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">@item.Name</h5>
                        <h6 class="card-title">@item.Price</h6>
                        <p class="card-text">@item.Description</p>
                        <div class="mt-auto">
                            <div class="row">
                                <div class="col-6">
                                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-primary w-100">Thêm vào &#10084</a>
                                </div>
                                <div class="col-6">
                                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-danger w-100">Mua hàng</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
@section Scripts {
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}
